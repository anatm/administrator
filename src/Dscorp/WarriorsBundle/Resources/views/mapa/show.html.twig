{% extends '::base.html.twig' %}

{% block body -%}
    <h1>mapa</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Urlimagenmapa</th>
                <td>{{ entity.urlimagenMapa }}</td>
            </tr>
            <tr>
                <th>Descripcionmapa</th>
                <td>{{ entity.descripcionMapa }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('mapa') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('mapa_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
