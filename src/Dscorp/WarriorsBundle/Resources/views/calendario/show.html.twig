{% extends '::base.html.twig' %}

{% block body -%}
    <h1>calendario</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Diacalendario</th>
                <td>{{ entity.diaCalendario }}</td>
            </tr>
            <tr>
                <th>Mescalendario</th>
                <td>{{ entity.mesCalendario }}</td>
            </tr>
            <tr>
                <th>Aniocalendario</th>
                <td>{{ entity.anioCalendario }}</td>
            </tr>
            <tr>
                <th>Titulocalendario</th>
                <td>{{ entity.tituloCalendario }}</td>
            </tr>
            <tr>
                <th>Descripcioncalendario</th>
                <td>{{ entity.descripcionCalendario }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('calendario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('calendario_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
