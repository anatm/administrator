{% extends '::base.html.twig' %}

{% block body -%}
    <h1>pagina list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titulopagina</th>
                <th>Urlimagenpagina</th>
                <th>Descripcionpagina</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pagina_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tituloPagina }}</td>
                <td>{{ entity.urlimagenPagina }}</td>
                <td>{{ entity.descripcionPagina }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pagina_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pagina_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pagina_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
